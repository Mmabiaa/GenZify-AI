
import React from "react";
import { useTheme } from "next-themes";

// This component is now a placeholder since we're enforcing dark mode only
export default function ThemeToggle() {
  return null;
}
